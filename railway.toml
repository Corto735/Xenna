[build]
builder = "dockerfile"

[deploy]
startCommand = "./web"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

# Pas de volume persistant nécessaire : la DB SQLite est reconstruite
# depuis les migrations à chaque démarrage (données de référence uniquement).
